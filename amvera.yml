meta:
  environment: python
  tool: pip

build:
  requirementsPath: requirements.txt

run:
  persistencePath: /data
  command: python manage.py migrate && gunicorn core.wsgi:application --bind 0.0.0.0:80
